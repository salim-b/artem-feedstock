context:
  version: "3.0.0"

package:
  name: artem
  version: ${{ version }}

source:
  url: https://github.com/FineFindus/artem/archive/refs/tags/v${{ version }}.tar.gz
  sha256: d180ff37691654744891de77d956c83cc334c82e2377d1cfe4b795234753781a

build:
  number: 1
  script:
    env:
      CARGO_PROFILE_RELEASE_LTO: fat
      CARGO_PROFILE_RELEASE_STRIP: symbols
    interpreter: nu
    content:
      - let prefix = $env.LIBRARY_PREFIX? | default $env.PREFIX
      # build main app and generate license data
      - cargo auditable install --path=. --root=($prefix) --no-track --locked --bins
      - cargo-bundle-licenses --format=yaml --output=./THIRDPARTY.yml
      # copy shell completion files and man page
      - mkdir ($prefix)/share/bash-completion/completions ($prefix)/share/fish/vendor_completions.d ($prefix)/share/zsh/site-functions ($prefix)/share/man/man1
      - let out_dir = glob target/*/release/build/artem-*/out | first
      - cp ($out_dir)/artem.bash ($prefix)/share/bash-completion/completions/artem
      - cp ($out_dir)/artem.fish ($prefix)/share/fish/vendor_completions.d/artem.fish
      - cp ($out_dir)/_artem     ($prefix)/share/zsh/site-functions/_artem
      - cp ($out_dir)/artem.1    ($prefix)/share/man/man1/artem.1

requirements:
  build:
    - ${{ stdlib('c') }}
    - ${{ compiler('c') }}
    - ${{ compiler('rust') }}
    - cargo-auditable
    - cargo-bundle-licenses
    - nushell

tests:
  - script:
      - artem --help
      - artem --version
  - package_contents:
      bin:
        - artem
      files:
        - if: unix
          then:
            - share/bash-completion/completions/artem
            - share/fish/vendor_completions.d/artem.fish
            - share/zsh/site-functions/_artem
            - share/man/man1/artem.1
        - if: win
          then:
            - Library/share/bash-completion/completions/artem
            - Library/share/fish/vendor_completions.d/artem.fish
            - Library/share/zsh/site-functions/_artem
            - Library/share/man/man1/artem.1
      strict: true

about:
  summary: Convert images from multiple formats (jpg, png, webp, etc.) to ASCII art
  description: |
    artem is a small CLI program written in Rust to easily convert images to ASCII art.
    It supports conversion from multiple image formats (jpg, png, webp, gif and many more)
    to ASCII art, with truecolor support by default.
  license: MPL-2.0
  license_file:
    - LICENSE
    - THIRDPARTY.yml
  homepage: https://github.com/FineFindus/artem
  repository: https://github.com/FineFindus/artem

extra:
  recipe-maintainers:
    - salim-b
